% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{calculate_metrics}
\alias{calculate_metrics}
\title{Compute scores from a CIF prediction array}
\usage{
calculate_metrics(
  cif,
  cr,
  eval_times,
  metrics = c("brier", "auc"),
  summary = c("ibs", "risks"),
  cens.method = "ipcw",
  cens.model = "km",
  cens.code = 0,
  se.fit = FALSE
)
}
\arguments{
\item{cif}{3-D numeric array with dimensions `[n, K, Tm]`, where `n` is
the number of subjects, `K` the number of competing causes, and `Tm` the
number of evaluation times.}

\item{cr}{A [cr_data()] object providing the test-set outcomes, time and
event variable names, and cause codes.}

\item{eval_times}{Numeric vector of evaluation times (length `Tm`).}

\item{metrics}{Character vector of metrics to compute. Supported values:
`"brier"` (Brier score), `"auc"` (time-dependent AUC), `"cidx_pec"`
(C-index via \pkg{pec}), `"cidx_survM"` (C-index via SurvM),
`"calib_measures"` (calibration summary statistics).}

\item{summary}{Character vector of Brier-score summaries (relevant only
when `"brier"` is in `metrics`): `"ibs"` for the integrated Brier score
and/or `"risks"` for per-risk summaries.}

\item{cens.method}{Censoring correction method passed to
`riskRegression::Score()` (default `"ipcw"`); relevant for `"brier"`
and `"auc"`.}

\item{cens.model}{Censoring model used for IPCW weighting (default
`"km"`); relevant for `"brier"` and `"auc"`.}

\item{cens.code}{Integer code denoting censored observations (default
`0`); relevant for `"calib_measures"`.}

\item{se.fit}{Logical; compute standard errors? (default `FALSE`);
relevant for `"brier"` and `"auc"`.}
}
\value{
A named list; elements present depend on the requested metrics:
  `cause_bs`, `cause_ibs`, `cause_auc`, `cause_cindex_pec`,
  `cause_cindex_survM`, `cause_calib_measures`.
}
\description{
Wraps `riskRegression::Score()`, `pec::cindex()`, and custom calibration
routines to compute one or more performance metrics from a 3-D array of
CIF predictions.
}
