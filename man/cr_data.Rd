% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cr_data.R
\name{cr_data}
\alias{cr_data}
\title{Prepare data for competing risks model fitting}
\usage{
cr_data(
  data,
  time_var,
  event_var,
  sort_by_time = TRUE,
  id_var = NULL,
  time_offset = 0,
  cens_code = 0L
)
}
\arguments{
\item{data}{A data frame.}

\item{time_var}{Name of the time column (must be numeric, no NAs).}

\item{event_var}{Name of the event/status column (numeric, integer, or
factor; 0 = censored; no NAs).}

\item{sort_by_time}{Logical; sort rows by ascending time (default \code{TRUE}).}

\item{id_var}{Optional name of the subject ID column.  If \code{NULL}
(default), a column named \code{"id"} is added automatically using the
original row numbers before sorting.  An error is raised if \code{"id"}
already exists in \code{data} and \code{id_var} is not supplied.}

\item{time_offset}{Non-negative numeric offset added to all times when the
minimum observed time is zero.  Must be \code{> 0} in that case; an
error is raised when it is \code{0} (the default).}

\item{cens_code}{Integer code denoting censored observations in
\code{event_var} (default \code{0}).}
}
\value{
A \code{cr_data} S4 object.
}
\description{
Validates inputs, coerces the event column to integer, records covariate
types, and optionally sorts by time.  Returns a \code{cr_data} object
consumed by model \code{fit} functions.
}
