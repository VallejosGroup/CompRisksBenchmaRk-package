% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_cv.R
\name{nested_cv_from_bench}
\alias{nested_cv_from_bench}
\title{Run nested cross-validation from a benchmark directory}
\usage{
nested_cv_from_bench(
  out_dir = "../BenchResults",
  model_key,
  cif_time_grid,
  metrics = c("Brier", "IBS", "tdAUC", "cindex_rmlt"),
  pred_horizons = NULL,
  grid = NULL,
  seed = 123L,
  verbose = TRUE,
  drop_cols_after_impute = NULL
)
}
\arguments{
\item{out_dir}{Path to the benchmark directory (default
`"../BenchResults"`).}

\item{model_key}{Character key of a model registered with
[register_cr_model()].}

\item{cif_time_grid}{Numeric vector of time points at which CIF predictions
are evaluated.}

\item{pred_horizons}{Numeric vector of prediction horizons passed to
[compute_metrics()]. Defaults to `cif_time_grid` if `NULL`.}

\item{grid}{For tunable models: a named list of hyperparameter
sequences to be expanded via `tidyr::expand_grid()`.  Ignored for
models with `needs_tuning = FALSE`.}

\item{seed}{Base random seed (outer fold `v` uses `seed + v`).}

\item{verbose}{Print progress messages.}

\item{drop_cols_after_impute}{Optional character vector of feature columns
to exclude from the model even after they have been used in imputation.}
}
\value{
A list of length `outer_folds`.  Each element is a named list
  containing `fold`, `model_key`, `cif_time_grid`, `best_cfg` (tunable models
  only), and `metrics` (`causes`, `cause_bs`, `cause_ibs`).
}
\description{
Reads pre-built nested CV splits from disk (as created by
[create_nested_splits()]), fits the requested model in each outer fold
(with optional inner-loop hyperparameter tuning), and returns per-fold
performance metrics.
}
\details{
For models with `needs_tuning = TRUE`, the inner loop evaluates each
hyperparameter configuration in `grid` using the Integrated Brier Score
and selects the best configuration per cause before refitting on the full
outer training set.
}
