% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{ibs_from_r}
\alias{ibs_from_r}
\title{IBS wrapper for use from Python or external processes}
\usage{
ibs_from_r(
  out,
  e_va,
  t_va,
  times,
  causes,
  time_col = "time",
  status_col = "event",
  cens.method = "ipcw",
  cens.model = "km",
  cens.code = 0L,
  force_sequential = TRUE,
  quiet = TRUE,
  log_file = NULL,
  debug = FALSE,
  debug_n = 5L
)
}
\arguments{
\item{out}{3-D numeric array `[n, K, Tm]`.}

\item{e_va}{Integer vector of observed event codes (length `n`).}

\item{t_va}{Numeric vector of observed times (length `n`).}

\item{times}{Numeric vector of evaluation times (length `Tm`).}

\item{causes}{Integer vector of cause codes (length `K`).}

\item{time_col}{Column name for time (default `"time"`).}

\item{status_col}{Column name for status (default `"event"`).}

\item{cens.method}{Passed to `Score()` (default `"ipcw"`).}

\item{cens.model}{Passed to `Score()` (default `"km"`).}

\item{cens.code}{Censoring code (default `0`).}

\item{force_sequential}{If `TRUE`, force sequential execution and
single-threaded BLAS (default `TRUE`).}

\item{quiet}{Suppress output (default `TRUE`).}

\item{log_file}{Optional path to a log file.}

\item{debug}{Return debug information? (default `FALSE`).}

\item{debug_n}{Number of rows to include in debug output.}
}
\value{
A numeric matrix `[K, Tm]` of IBS values, or a debug list if
  `debug = TRUE`.
}
\description{
Computes the Integrated Brier Score for each cause in a 3-D CIF array
using `riskRegression::Score()`.
}
