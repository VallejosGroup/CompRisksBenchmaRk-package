% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{impute_split}
\alias{impute_split}
\title{Impute missing values in a train/test split}
\usage{
impute_split(
  train,
  test,
  feature_cols,
  method = c("none", "median_mode", "mice"),
  m = 1L,
  maxit = 100L,
  seed = 1L,
  drop_cols_after_impute = NULL
)
}
\arguments{
\item{train}{Training data frame.}

\item{test}{Test (or validation) data frame.}

\item{feature_cols}{Character vector of feature column names to impute.}

\item{method}{One of `"none"`, `"median_mode"`, or `"mice"`.}

\item{m}{Number of imputed datasets (only used when `method = "mice"`).}

\item{maxit}{Number of MICE iterations (only used when `method = "mice"`).}

\item{seed}{Integer seed for MICE.}

\item{drop_cols_after_impute}{Optional character vector of column names
to drop after imputation.}
}
\value{
A list with elements:
  \describe{
    \item{m}{Number of imputed datasets.}
    \item{imputations}{A list of length `m`, each element being a list
      with `$train` and `$test` data frames.}
  }
}
\description{
Handles three strategies: `"none"` (passthrough), `"median_mode"`
(numeric columns filled with training median, factor/logical columns
filled with training mode), and `"mice"` (multiple imputation via the
**mice** package).
}
